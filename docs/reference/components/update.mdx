---
title: Update
description: Release block for changelog entries. Version label, date, optional tag chips, MDX body.
icon: "megaphone"
---

# Update

`<Update>` renders a single entry in a changelog. Heading on the left, date on the right, tag chips next to the heading, full MDX body underneath.

This is the only "callout-shaped" component that takes structured props. Everything else in the [callouts group](/reference/components/note) is content-only.

## When to use

- Changelog pages. `tangly add changelog 1.2.0` scaffolds one for you.
- "What's new" sections at the top of a guide.
- Release roadmaps.

For inline release-flavored asides ("New in v0.3"), a `<Tag>` or `<Badge>` reads cleaner.

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `label` | `string` | — | Version or release name. Renders as the heading. |
| `description` | `string` | — | Subtitle, typically `YYYY-MM-DD`. |
| `tags` | `string[]` | `[]` | Tag chips rendered to the right of the heading. |

## Examples

### Basic release

```mdx
<Update label="v0.2.0" description="2026-04-29" tags={["alpha"]}>
  ## Initial release

  Renders Mintlify projects unmodified.
</Update>
```

<Update label="v0.2.0" description="2026-04-29" tags={["alpha"]}>
  ## Initial release

  Renders Mintlify projects unmodified.
</Update>

### Multi-tag

```mdx
<Update label="v0.3.0" description="2026-06-15" tags={["minor", "ai-agents"]}>
  - New `<PackageManager>` component
  - `aiContext` frontmatter for agent hints
  - Bun 1.3 minimum
</Update>
```

## Scaffolding

```bash
tangly add changelog 1.2.0
```

Creates `changelog/1.2.0.mdx` with the `<Update>` block ready to fill in.

## Source

[`packages/theme-ui/src/components/Update.astro`](https://github.com/tanglydocs/tangly/blob/main/packages/theme-ui/src/components/Update.astro)
