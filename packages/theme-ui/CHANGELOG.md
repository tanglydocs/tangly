# @tanglydocs/theme-ui

## 0.0.7

### Patch Changes

- fix(build): sync `bun.lock` with workspace versions + new `template-starter` workspace; bump CI to bun 1.3.11; `bun run version` now runs `bun install` so future releases keep the lockfile in step.
- Updated dependencies
  - tangly@0.0.7
  - @tanglydocs/schema@0.0.7
